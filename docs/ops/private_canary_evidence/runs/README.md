# Private canary evidence runs

Archives created by [`scripts/laptop_private_canary.sh`](../../../scripts/laptop_private_canary.sh).

Each `cycle_N/` holds tip/start/end, per-venue logs or skip notes, and `result.txt`.

**Honesty:** laptop only; not scheduled beta; no order placement; secrets never archived.
