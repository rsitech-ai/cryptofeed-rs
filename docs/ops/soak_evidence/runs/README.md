# Soak run archives

Created by `scripts/laptop_soak.sh` as `synthetic_<UTC>/` or `live_<UTC>/`.

Each run directory holds `rss_samples.csv`, metrics snapshots, daemon log, and config copies.

**Honesty:** bounded laptop soaks here are **not** multi-day and **not** stable.
